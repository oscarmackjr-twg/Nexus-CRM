---
phase: 01-ui-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/ContactDetailPage.jsx
  - frontend/src/pages/CompanyDetailPage.jsx
  - frontend/src/pages/ContactsPage.jsx
  - frontend/src/pages/DealDetailPage.jsx
autonomous: true
requirements: [UI-02]

must_haves:
  truths:
    - "All in-scope pages use text-3xl font-semibold for primary headings"
    - "Detail pages have a clear page-level heading visible above the content"
    - "Card padding and row spacing are consistent across all screens"
    - "No visual mismatches in heading sizes, subtitle styles, or card layout between pages"
  artifacts:
    - path: "frontend/src/pages/ContactDetailPage.jsx"
      provides: "Consistent heading size (text-3xl) in sidebar card"
      contains: "text-3xl"
    - path: "frontend/src/pages/CompanyDetailPage.jsx"
      provides: "Page-level heading for company name"
      contains: "text-3xl font-semibold"
    - path: "frontend/src/pages/ContactsPage.jsx"
      provides: "Consistent spacing patterns"
    - path: "frontend/src/pages/DealDetailPage.jsx"
      provides: "Consistent spacing patterns"
  key_links:
    - from: "all in-scope pages"
      to: "design system"
      via: "Tailwind classes"
      pattern: "text-3xl font-semibold"
---

<objective>
Audit and fix spacing, typography, and layout consistency across Contact, Company, and Deal pages.

Purpose: Ensures all in-scope screens are visually indistinguishable in quality (per D-05) using the existing shadcn/ui + Tailwind design system (per D-06). No new design language introduced.
Output: Four page files with consistent heading sizes, subtitle patterns, card padding, and row spacing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ui-polish/01-CONTEXT.md
@.planning/phases/01-ui-polish/01-RESEARCH.md

@frontend/src/pages/ContactDetailPage.jsx
@frontend/src/pages/CompanyDetailPage.jsx
@frontend/src/pages/ContactsPage.jsx
@frontend/src/pages/DealDetailPage.jsx
@frontend/src/pages/CompaniesPage.jsx
@frontend/src/pages/DashboardPage.jsx

<interfaces>
<!-- Established heading pattern (from DashboardPage, CompaniesPage, ContactsPage) -->
```jsx
// Standard page header — ALL pages must use this pattern
<div>
  <h1 className="text-3xl font-semibold">{pageTitle}</h1>
  <p className="text-muted-foreground">{subtitle}</p>
</div>
```

<!-- Standard in-page row item -->
```jsx
<div className="rounded-xl bg-muted/40 px-4 py-3">...</div>
```

<!-- Standard card pattern -->
```jsx
<Card>
  <CardHeader><CardTitle>{title}</CardTitle></CardHeader>
  <CardContent>{children}</CardContent>
</Card>
```

<!-- Known inconsistencies found in codebase audit: -->
<!-- ContactDetailPage.jsx line 121: uses text-2xl (should be text-3xl) -->
<!-- CompanyDetailPage.jsx: has NO page-level h1 heading at all -->
<!-- ContactsPage.jsx: consistent already -->
<!-- DealDetailPage.jsx: uses editable Input for title, ok pattern -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ContactDetailPage and CompanyDetailPage headings and layout</name>
  <files>frontend/src/pages/ContactDetailPage.jsx, frontend/src/pages/CompanyDetailPage.jsx</files>
  <read_first>
    - frontend/src/pages/ContactDetailPage.jsx (full file — sidebar card heading uses text-2xl on line 121)
    - frontend/src/pages/CompanyDetailPage.jsx (full file — no page-level h1, company name only in CardTitle)
    - frontend/src/pages/DashboardPage.jsx (reference — correct pattern: text-3xl font-semibold h1)
    - frontend/src/pages/CompaniesPage.jsx (reference — correct pattern)
  </read_first>
  <action>
    Per D-05, fix the two detail pages that deviate from the established heading and layout patterns.

    **ContactDetailPage.jsx — two fixes:**

    1. **Line 121** — Change `text-2xl` to `text-3xl` in the sidebar contact name:
       Change: `<p className="text-2xl font-semibold">`
       To: `<p className="text-3xl font-semibold">`

    2. **Wrap the outer grid in a `space-y-6` container with a page header** — currently the page starts directly with the grid layout (`<div className="grid gap-6 xl:grid-cols-[320px,1fr]">`). Add a wrapper so the structure is:
       ```jsx
       <div className="space-y-6">
         <div>
           <h1 className="text-3xl font-semibold">{contact.first_name} {contact.last_name}</h1>
           <p className="text-muted-foreground">{contact.company_name || 'No company linked'}</p>
         </div>
         <div className="grid gap-6 xl:grid-cols-[320px,1fr]">
           {/* existing sidebar and main content */}
         </div>
       </div>
       ```
       Note: The existing h1 on line 145 (inside the right column) can be removed since the page now has a top-level heading. Keep the "Log activity" button — move it next to the heading:
       ```jsx
       <div className="flex items-center justify-between">
         <div>
           <h1 className="text-3xl font-semibold">{contact.first_name} {contact.last_name}</h1>
           <p className="text-muted-foreground">{contact.company_name || 'No company linked'}</p>
         </div>
         <Button onClick={() => setActivityOpen(true)}><PlusCircle className="h-4 w-4" />Log activity</Button>
       </div>
       ```
       Then remove the duplicate heading+button block that was inside the right column (lines 144-146).

    **CompanyDetailPage.jsx — add page heading:**

    Currently the page renders directly into the grid with no h1. Wrap in `space-y-6` and add a heading:
    ```jsx
    <div className="space-y-6">
      <div>
        <h1 className="text-3xl font-semibold">{company.name}</h1>
        <p className="text-muted-foreground">{company.industry || 'Unknown industry'} {company.website ? `\u2022 ${company.website}` : ''}</p>
      </div>
      <div className="grid gap-6 xl:grid-cols-[320px,1fr]">
        {/* existing sidebar and main columns */}
      </div>
    </div>
    ```
  </action>
  <verify>
    <automated>cd /Users/oscarmack/OpenClaw/nexus-crm && grep -c "text-2xl" frontend/src/pages/ContactDetailPage.jsx && grep -c "text-3xl font-semibold" frontend/src/pages/ContactDetailPage.jsx && grep -c "text-3xl font-semibold" frontend/src/pages/CompanyDetailPage.jsx</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/ContactDetailPage.jsx contains 0 occurrences of `text-2xl` (was 1, now fixed to text-3xl)
    - frontend/src/pages/ContactDetailPage.jsx contains `<h1 className="text-3xl font-semibold">` as a page-level heading
    - frontend/src/pages/ContactDetailPage.jsx outer wrapper is `<div className="space-y-6">`
    - frontend/src/pages/ContactDetailPage.jsx does NOT have a duplicate h1 inside the right column grid area
    - frontend/src/pages/CompanyDetailPage.jsx contains `<h1 className="text-3xl font-semibold">{company.name}</h1>`
    - frontend/src/pages/CompanyDetailPage.jsx outer wrapper is `<div className="space-y-6">`
  </acceptance_criteria>
  <done>ContactDetailPage and CompanyDetailPage both have consistent page-level headings matching the text-3xl font-semibold pattern used by Dashboard, Contacts, Companies, and all other pages. No text-2xl remains.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and normalize ContactsPage and DealDetailPage spacing</name>
  <files>frontend/src/pages/ContactsPage.jsx, frontend/src/pages/DealDetailPage.jsx</files>
  <read_first>
    - frontend/src/pages/ContactsPage.jsx (full file — verify header pattern and card spacing)
    - frontend/src/pages/DealDetailPage.jsx (full file — verify grid and card spacing)
    - frontend/src/pages/DashboardPage.jsx (reference pattern)
  </read_first>
  <action>
    Per D-05 and D-06, audit and normalize spacing on the two remaining in-scope pages. These pages are mostly consistent already but need minor alignment.

    **ContactsPage.jsx — minor fixes:**
    - Already uses `space-y-6` wrapper and `text-3xl font-semibold` heading -- GOOD.
    - Verify the filter card and table card both use the standard `Card > CardHeader > CardContent` pattern -- they do.
    - No changes expected unless a spacing inconsistency is found during read. If the page is already consistent with the pattern, leave it unchanged and note in summary.

    **DealDetailPage.jsx — spacing normalization:**
    - Already uses `space-y-6` wrapper -- GOOD.
    - The title uses an `<Input>` element with `text-3xl font-semibold` for inline editing -- this is an intentional pattern for deal names, leave it.
    - Check that the three-column grid (`xl:grid-cols-[280px,1fr,320px]`) uses consistent `gap-6` -- it does.
    - Check that `CardContent` sections use consistent `space-y-4` -- they do.
    - The `DealScoreCard` component (lines 15-31) uses `Card > CardHeader > CardContent` pattern with `space-y-4` -- consistent.
    - Verify `rounded-xl bg-muted/40` pattern on row items (activity timeline items, score factors) -- confirm and fix any that use different padding.
    - Activity timeline items on line 110 use `p-4` while the standard pattern is `px-4 py-3`. Normalize to `px-4 py-3` to match DashboardPage row items.
    - Score factor items on line 30 use `px-3 py-2 text-sm` -- these are smaller items, intentionally compact, leave as-is.

    **For both files:** After reading, if no changes are needed, create the file unchanged. The audit itself is the value -- confirming consistency.
  </action>
  <verify>
    <automated>cd /Users/oscarmack/OpenClaw/nexus-crm && grep -c "text-3xl font-semibold" frontend/src/pages/DealDetailPage.jsx && grep "space-y-6" frontend/src/pages/ContactsPage.jsx | head -1 && grep "space-y-6" frontend/src/pages/DealDetailPage.jsx | head -1</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/ContactsPage.jsx outer wrapper contains `space-y-6`
    - frontend/src/pages/ContactsPage.jsx heading contains `text-3xl font-semibold`
    - frontend/src/pages/DealDetailPage.jsx outer wrapper contains `space-y-6`
    - frontend/src/pages/DealDetailPage.jsx activity timeline row items use `px-4 py-3` (not `p-4`)
    - Neither file introduces any new CSS classes outside the existing Tailwind scale (per D-06)
  </acceptance_criteria>
  <done>All six in-scope pages (Login, Dashboard, Contacts list, Contact detail, Company detail, Deal detail) use consistent heading sizes, subtitle patterns, and card/row spacing. No visual mismatches remain.</done>
</task>

</tasks>

<verification>
1. All in-scope pages use `text-3xl font-semibold` for primary headings:
   `grep -l "text-3xl font-semibold" frontend/src/pages/{ContactDetailPage,CompanyDetailPage,ContactsPage,DealDetailPage,DashboardPage}.jsx`
   Should return all 5 files.
2. No `text-2xl` heading sizes in detail pages:
   `grep -c "text-2xl" frontend/src/pages/{ContactDetailPage,CompanyDetailPage,DealDetailPage}.jsx`
   Should return 0 for all.
3. `cd frontend && npm test -- --run` passes (no component render breaks).
</verification>

<success_criteria>
- All in-scope page headings use text-3xl font-semibold consistently
- ContactDetailPage and CompanyDetailPage have page-level h1 headings (not buried in cards/columns)
- Row item padding is consistent (px-4 py-3 pattern)
- No new design tokens or CSS variables introduced (per D-06)
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-polish/01-02-SUMMARY.md`
</output>
