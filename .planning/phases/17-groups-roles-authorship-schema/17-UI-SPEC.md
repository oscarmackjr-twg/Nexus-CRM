---
phase: 17
slug: groups-roles-authorship-schema
status: approved
shadcn_initialized: false
preset: none
created: 2026-04-06
reviewed_at: 2026-04-06
---

# Phase 17 — UI Design Contract

> Visual and interaction contract for Phase 17: Groups, Roles & Authorship Schema.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (manual shadcn-style — components.json absent; shadcn/ui components present in `src/components/ui/`) |
| Preset | not applicable |
| Component library | shadcn/ui (radix primitives, manually installed) |
| Icon library | lucide-react (already used: `Pencil`, `RotateCcw`, `X`, `ChevronUp`, `ChevronDown`, `ChevronsUpDown`, `Users`, `UsersRound`) |
| Font | Montserrat, ui-sans-serif, system-ui, sans-serif (source: `styles.css` body declaration) |

> Source: codebase scan — `frontend/src/styles.css`, `frontend/src/components/ui/`, `frontend/src/components/DataGrid.jsx`, `frontend/src/pages/AdminPage.jsx`

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline badge padding |
| sm | 8px | Table cell gap, compact field spacing |
| md | 16px | Card padding, form field spacing, dialog body padding |
| lg | 24px | Section padding, card header padding |
| xl | 32px | Page-level section separation |
| 2xl | 48px | Major layout gaps (page header to content) |
| 3xl | 64px | Not used in this phase |

Exceptions: None for this phase. Existing `p-6` (24px) pattern from `AdminPage.jsx` CardContent is the standard admin page padding — preserve it.

> Source: existing `AdminPage.jsx` (`p-6`, `px-4`, `py-2`) and DataGrid row density (`py-2`) patterns.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (text-sm) | 400 (regular) | 1.5 |
| Label | 12px (text-xs) | 400 (regular) | 1.4 |
| Column header | 12px (text-xs) | 600 (semibold) | 1.0 |
| Dialog title | 16px (text-base) | 600 (semibold) | 1.2 |
| Page heading / Card title | 20px (text-xl) | 600 (semibold) | 1.2 |

> Source: RESEARCH.md confirms DataGrid uses `text-sm` throughout; column headers use `text-xs` + `tracking-wide` (GRID-04). AdminPage.jsx `CardTitle` maps to text-xl semibold per shadcn Card defaults. `DialogTitle` uses `font-semibold` at 16px per shadcn Dialog defaults. Body weight 400 and heading weight 600 are the two declared weights — no third weight.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `hsl(220 33% 98%)` — `--background` | Page background, content area, dialog backgrounds |
| Secondary (30%) | `hsl(0 0% 100%)` — `--card` (`#ffffff`) | Card surfaces, table rows, sidebar white background |
| Accent (10%) | `#1a3868` — `--color-brand` / `--primary` | Active tab underline, primary button fill, nav left-border indicator, focus rings |
| Destructive | `hsl(0 84% 60%)` — `--danger` | Deactivate group confirmation button only |
| Muted | `hsl(220 9% 46%)` — `--muted-foreground` | Column headers, field labels, secondary meta text (group member count, role badge label) |

Accent reserved for:
- Primary "Add User" / "Add Group" / "Update User" / "Create Group" / "Rename Group" button fill in create/edit dialogs
- Active state of sidebar nav items ("Users", "Groups" links — navy `border-l-4` + navy text)
- Active tab underline on any tabbed views
- Focus rings on all interactive elements (`--ring: 217 60% 25%`)
- Hover state on deactivate/edit row action icons

> Source: `frontend/src/styles.css` CSS variable definitions. STATE.md key decision `01-ui-polish-03` (light theme, white content area, dark sidebar reversed). Sidebar remains `bg-slate-900` (dark blue) per Phase 8 — new nav entries follow the same pattern.

---

## Component Inventory

All components used in this phase are already installed. No new npm installs required.

| Component | Import Path | Used For |
|-----------|-------------|----------|
| `DataGrid` | `@/components/DataGrid` | User list table, Group list table |
| `Pagination` | `@/components/Pagination` | Paginated user/group lists |
| `Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle` | `@/components/ui/dialog` | Create user modal, Edit user modal, Create group modal, Rename group modal |
| `Table`, `TableHeader`, `TableHead`, `TableBody`, `TableRow`, `TableCell` | `@/components/ui/table` | Fallback if DataGrid not used; Group members sub-list |
| `Button` | `@/components/ui/button` | "Add User", "Add Group", "Update User", "Create Group", "Rename Group", "Deactivate" actions |
| `Input` | `@/components/ui/input` | Group name field in create/rename dialog |
| `Label` | `@/components/ui/label` | Form field labels in dialogs |
| `Select` (shadcn) | `@/components/ui/select` | Role dropdown in user edit modal; Group dropdown in user edit modal |
| `Badge` | `@/components/ui/badge` | Role badge on user list rows (e.g., "Supervisor", "Admin") |
| `Card`, `CardContent`, `CardHeader`, `CardTitle` | `@/components/ui/card` | Page wrapper card for both admin pages |
| `Skeleton` | `@/components/ui/skeleton` | Loading state for user list and group list |
| `Switch` | `@/components/ui/switch` | "Show inactive groups" toggle on Group Management page |

> Source: CONTEXT.md § Code Context; RESEARCH.md § Standard Stack; direct `AdminPage.jsx` import analysis.

---

## Page Structure

### `/admin/users` — User Management Page

Focal point: the `Add User` accent-filled button in CardHeader.

```
<Card>
  <CardHeader>
    <CardTitle>User Management</CardTitle>
    <Button variant="default">Add User</Button>   ← accent fill; primary focal point
  </CardHeader>
  <CardContent p-6>
    <DataGrid
      columns={[Name, Email, Group, Role, Status, Actions]}
      emptyHeading="No users found"
      emptyBody="Add your first user to get started."
    />
  </CardContent>
</Card>
```

**Columns:**
- Name — 14px regular, link to no detail page (not clickable in Phase 17)
- Email — 14px regular, muted
- Group — 14px regular (group name or "—" if unassigned)
- Role — `<Badge>` with role label (see Role Badge spec below)
- Status — Active / Inactive badge
- Actions — Pencil icon (`aria-label="Edit user"`) and X icon (`aria-label="Deactivate user"`), visible on row hover only

**Role Badge variants:**

| Role value | Badge label | Badge variant |
|------------|-------------|---------------|
| `admin` | Admin | `default` (navy fill) |
| `supervisor` | Supervisor | `secondary` (muted purple) |
| `principal` | Principal | `outline` (navy border) |
| `regular_user` | Regular User | `outline` (gray border) |

### `/admin/groups` — Group Management Page

Focal point: the `Add Group` accent-filled button in CardHeader.

```
<Card>
  <CardHeader>
    <CardTitle>Group Management</CardTitle>
    <div class="flex gap-2">
      <Switch label="Show inactive" />
      <Button variant="default">Add Group</Button>   ← accent fill; primary focal point
    </div>
  </CardHeader>
  <CardContent p-6>
    <DataGrid
      columns={[Group Name, Members, Status, Actions]}
      emptyHeading="No groups found"
      emptyBody="Create your first group to start organizing users."
    />
  </CardContent>
</Card>
```

**Columns:**
- Group Name — 14px regular
- Members — 14px regular, number (0 if empty)
- Status — Active / Inactive badge
- Actions — Pencil icon (`aria-label="Rename group"`) and X icon (`aria-label="Deactivate group"`), visible on row hover only

### Sidebar Additions (Layout.jsx ADMIN section)

Add two nav entries under existing ADMIN group in `Layout.jsx`:
```
{ name: 'Users', href: '/admin/users', icon: Users }       ← lucide Users
{ name: 'Groups', href: '/admin/groups', icon: UsersRound } ← lucide UsersRound
```
Nav style: identical to all existing nav items — active state navy `border-l-4`, navy text, `bg-gray-50` highlight.

---

## Interaction Patterns

### Create/Edit Dialogs

All modals follow the established `AdminPage.jsx` Dialog pattern:
- Dialog opens centered, max-width `sm` (`max-w-sm`)
- `DialogHeader` with `DialogTitle` (16px semibold, line-height 1.2)
- Form fields: `Label` above `Input` or `Select`, `gap-4` vertical spacing
- Footer: `Button variant="outline"` (dismissal) + `Button variant="default"` (confirm action) — right-aligned, `gap-2` between

**User Edit Modal fields:**
1. Name (read-only `Input`, disabled — display only)
2. Email (read-only `Input`, disabled — display only)
3. Role — `Select` with 4 options: Regular User, Supervisor, Principal, Admin
4. Group — `Select` populated from `/admin/groups` list (active groups only)
5. Footer: `Discard Changes` (outline) + `Update User` (default accent)

**Create User Modal fields:**
1. Name — `Input` required
2. Email — `Input` required
3. Password — `Input type="password"` required
4. Role — `Select`, defaults to "Regular User"
5. Group — `Select` (active groups only), optional
6. Footer: `Never Mind` (outline) + `Add User` (default accent)

**Create Group Modal fields:**
1. Group Name — `Input` required
2. Footer: `Never Mind` (outline) + `Create Group` (default accent)

**Rename Group Modal fields:**
1. Group Name — `Input` pre-filled with current name
2. Footer: `Discard` (outline) + `Rename Group` (default accent)

### Loading State

Use `<Skeleton>` rows (height 32px, `rounded-md`, 3–5 rows) in place of the DataGrid while queries load. Pattern matches existing `AdminPage.jsx` Skeleton usage.

### Toast Notifications

Use existing `sonner` toast pattern from `AdminPage.jsx`:
- Success: `toast.success("User updated")` / `toast.success("Group created")` etc.
- Error: `toast.error(err.response?.data?.detail || "Failed to save changes")`

### Role Dropdown — Friendly Labels

Use `ROLE_LABELS` constant (RESEARCH.md Pattern 6):
```js
const ROLE_LABELS = {
  admin: 'Admin',
  supervisor: 'Supervisor',
  principal: 'Principal',
  regular_user: 'Regular User',
};
```
Display friendly label in all UI surfaces — never raw snake_case values.

### Pagination

`DataGrid` + `Pagination` component, default page size 20, options [10, 20, 50]. Consistent with existing DataGrid usage.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Users page title | User Management |
| Groups page title | Group Management |
| Primary CTA — add user | Add User |
| Primary CTA — add group | Add Group |
| Primary CTA — save changes (user edit modal) | Update User |
| Primary CTA — create group | Create Group |
| Primary CTA — rename group | Rename Group |
| Secondary CTA — dismiss user edit modal | Discard Changes |
| Secondary CTA — dismiss create modals (user, group) | Never Mind |
| Secondary CTA — dismiss rename group modal | Discard |
| User list empty state heading | No users found |
| User list empty state body | Add your first user to get started. |
| Group list empty state heading | No groups found |
| Group list empty state body | Create your first group to start organizing users. |
| Error state (API failure) | Failed to load data. Check your connection and try again. |
| Save error (mutation failure) | Failed to save changes. {server detail if available} |
| Role column unassigned | — (em dash, consistent with DETAIL-03 pattern) |
| Group column unassigned | — (em dash) |
| Deactivate group confirmation | Deactivate "{group name}"? Users in this group will lose their group assignment. This cannot be undone from the UI. |
| Deactivate user confirmation | Deactivate {user name}? They will no longer be able to log in. |

---

## Destructive Actions

| Action | Trigger | Confirmation Pattern |
|--------|---------|---------------------|
| Deactivate group | X icon on group row (`aria-label="Deactivate group"`, hover-visible) | Inline `window.confirm()` dialog — consistent with low-ceremony admin pattern. Copy: "Deactivate '{group name}'? Users in this group will lose their group assignment." |
| Deactivate user | X icon on user row (`aria-label="Deactivate user"`, hover-visible) | Inline `window.confirm()` dialog. Copy: "Deactivate {user name}? They will no longer be able to log in." |

> Rationale: `window.confirm()` matches the effort level of the existing admin actions (no shadcn AlertDialog installed). If shadcn AlertDialog is added in a future phase, migrate at that time.

Destructive button color: `--danger` (`hsl(0 84% 60%)`) — red, used only on the confirmation prompt's confirm button, not on the row icon itself.

---

## Access Gate

Both new admin pages must gate on `user?.role === 'admin'`. Non-admin users see:

```jsx
<Card>
  <CardContent className="p-6 text-sm text-muted-foreground">
    Admin access is restricted to admins.
  </CardContent>
</Card>
```

> Source: CONTEXT.md D-12, D-13; RESEARCH.md Pitfall 5 (stale role check in AdminPage.jsx must also be updated to `'admin'`).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Dialog, Table, Button, Input, Label, Select, Badge, Card, Skeleton, Switch, Tabs | not required |
| Third-party | none | not applicable |

No third-party registry blocks are needed or declared for this phase.

---

## Design Decisions Pre-Populated From Upstream

| Decision | Source | Value |
|----------|--------|-------|
| Brand color `#1a3868` navy | STATE.md key decision `07-01` | Primary button, active nav, focus rings |
| Light theme: white content area, dark sidebar | STATE.md key decision `01-ui-polish-03` | Page bg `hsl(220 33% 98%)`, card `#ffffff`, sidebar `bg-slate-900` |
| Font: Montserrat | `styles.css` body declaration | All text |
| Separate sub-routes `/admin/users`, `/admin/groups` | CONTEXT.md D-12 | Not tabs inside AdminPage |
| Nav entries under ADMIN section | CONTEXT.md D-13 | Layout.jsx navGroups ADMIN group |
| Role dropdown shows friendly labels | CONTEXT.md Claude's Discretion | "Regular User" not `regular_user` |
| DataGrid + Dialog pattern | CONTEXT.md § Code Context | Consistent with AdminPage.jsx |
| Em dash for null values | REQUIREMENTS.md DETAIL-03 | `—` for unassigned group/role |
| `include_inactive` toggle for groups | RESEARCH.md Pattern 5 | Mirrors `/admin/ref-data` pattern |
| Active groups only in user edit role/group dropdowns | RESEARCH.md Pitfall 4 | Filter `is_active=true` on group select |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
