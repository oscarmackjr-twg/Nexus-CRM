---
phase: 07-brand-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/index.html
  - frontend/src/styles.css
autonomous: true
requirements:
  - BRAND-01
  - BRAND-02
  - BRAND-03

must_haves:
  truths:
    - "Every primary button and focus ring renders navy (#1a3868 / rgb(26, 56, 104)) instead of indigo"
    - "Body text renders in Montserrat with system-ui fallback on all pages"
    - "CSS variables --color-brand, --color-brand-hover, --color-page-bg, --color-content-bg, --color-text are defined in :root and .dark"
    - "No hardcoded indigo, purple, or violet Tailwind classes exist in frontend/src/"
  artifacts:
    - path: "frontend/src/styles.css"
      provides: "Navy CSS variables in :root and .dark, POC-pattern vars, Montserrat font-family, navy surface-grid rgba"
      contains: "--primary: 217 60% 25%"
    - path: "frontend/index.html"
      provides: "Google Fonts Montserrat CDN link with preconnect"
      contains: "fonts.googleapis.com"
  key_links:
    - from: "frontend/src/styles.css"
      to: "frontend/tailwind.config.js"
      via: "CSS variable cascade — --primary consumed by hsl(var(--primary)) in tailwind config"
      pattern: "--primary: 217 60% 25%"
    - from: "frontend/index.html"
      to: "frontend/src/styles.css"
      via: "Google Fonts CDN loads Montserrat, body font-family references it"
      pattern: "Montserrat"
---

<objective>
Establish the TWG brand foundation globally: navy color palette via CSS variables, Montserrat font via Google Fonts, and POC-pattern semantic tokens.

Purpose: This is the prerequisite for all v1.1 UI phases (8-12). Every button, ring, and text element must reflect the TWG brand baseline before component-level work begins.
Output: Updated `frontend/src/styles.css` and `frontend/index.html` — two files that cascade the brand to every page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-brand-foundation/07-CONTEXT.md
@.planning/phases/07-brand-foundation/07-RESEARCH.md
@.planning/phases/07-brand-foundation/07-UI-SPEC.md

<interfaces>
<!-- From frontend/tailwind.config.js — executor must NOT edit this file -->
<!-- All color utilities route through CSS vars via hsl(var(--...)) -->
primary: 'hsl(var(--primary))',
ring: 'hsl(var(--ring))',
<!-- Changing --primary and --ring in styles.css cascades to all bg-primary, text-primary, ring-primary, border-primary usages automatically -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS variable remapping + POC tokens + font-family + surface-grid</name>
  <files>frontend/src/styles.css</files>
  <read_first>
    - frontend/src/styles.css (current state — 67 lines, HSL triplet format without hsl() wrapper)
    - frontend/tailwind.config.js (verify hsl(var(--primary)) wiring — DO NOT edit this file)
    - .planning/phases/07-brand-foundation/07-RESEARCH.md (Pattern 1-4, Pitfall 1-4)
    - .planning/phases/07-brand-foundation/07-UI-SPEC.md (Color section — exact before/after values)
  </read_first>
  <action>
Edit `frontend/src/styles.css` with ALL of the following changes in a single edit:

**1. :root block — remap existing tokens (BRAND-01, per D-02, D-03):**
Change line 13 `--ring: 239 84% 67%;` to `--ring: 217 60% 25%;`
Change line 14 `--primary: 239 84% 67%;` to `--primary: 217 60% 25%;`

**2. :root block — add POC-pattern vars after --muted-foreground (BRAND-03, per D-02 through D-05):**
Add these 5 new variables after `--muted-foreground: 220 9% 46%;`:
```
    /* POC-pattern semantic tokens (BRAND-03) */
    --color-brand: #1a3868;
    --color-brand-hover: #1e4080;
    --color-page-bg: #ffffff;
    --color-content-bg: #ffffff;
    --color-text: hsl(222, 47%, 11%);
```

**3. .dark block — remap existing tokens (per D-06):**
Change `--ring: 239 84% 67%;` to `--ring: 217 60% 25%;`
Change `--primary: 239 84% 67%;` to `--primary: 217 60% 25%;`

**4. .dark block — add POC-pattern vars after --muted-foreground (per D-06):**
Add the same 5 POC-pattern variables (identical values — dark mode is currently disabled, same navy is safe per D-06).

**5. body font-family (BRAND-02, per D-01):**
Change line 52 from:
`font-family: "IBM Plex Sans", ui-sans-serif, system-ui, sans-serif;`
to:
`font-family: 'Montserrat', ui-sans-serif, system-ui, sans-serif;`

**6. surface-grid rgba (BRAND-01, per D-08):**
Change line 63 from:
`linear-gradient(to right, rgba(99, 102, 241, 0.06) 1px, transparent 1px),`
to:
`linear-gradient(to right, rgba(26, 56, 104, 0.06) 1px, transparent 1px),`
Do NOT change the cyan rgba on line 64 — leave `rgba(6, 182, 212, 0.05)` as-is.

**CRITICAL FORMAT RULE:** `--primary` and `--ring` use bare HSL triplet format WITHOUT `hsl()` wrapper and WITHOUT commas: `217 60% 25%` — NOT `hsl(217, 60%, 25%)`. The `hsl()` wrapper lives in tailwind.config.js. Breaking this format causes Tailwind to emit `hsl(hsl(...))` and renders transparent/black.
  </action>
  <verify>
    <automated>grep -c "217 60% 25%" frontend/src/styles.css | grep -q "4" && grep -c "color-brand" frontend/src/styles.css | grep -q "2" && grep "font-family" frontend/src/styles.css | grep -q "Montserrat" && grep "surface-grid" -A2 frontend/src/styles.css | grep -q "26, 56, 104" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/styles.css` contains `--primary: 217 60% 25%;` exactly 2 times (once in :root, once in .dark)
    - `frontend/src/styles.css` contains `--ring: 217 60% 25%;` exactly 2 times (once in :root, once in .dark)
    - `frontend/src/styles.css` contains `--color-brand: #1a3868;` exactly 2 times
    - `frontend/src/styles.css` contains `--color-brand-hover: #1e4080;` exactly 2 times
    - `frontend/src/styles.css` contains `--color-page-bg: #ffffff;` exactly 2 times
    - `frontend/src/styles.css` contains `--color-content-bg: #ffffff;` exactly 2 times
    - `frontend/src/styles.css` contains `--color-text: hsl(222, 47%, 11%);` exactly 2 times
    - `frontend/src/styles.css` contains `font-family: 'Montserrat', ui-sans-serif, system-ui, sans-serif;`
    - `frontend/src/styles.css` does NOT contain `IBM Plex Sans`
    - `frontend/src/styles.css` does NOT contain `239 84% 67%`
    - `frontend/src/styles.css` contains `rgba(26, 56, 104, 0.06)` (navy surface-grid)
    - `frontend/src/styles.css` does NOT contain `rgba(99, 102, 241` (old indigo surface-grid)
    - `frontend/src/styles.css` still contains `rgba(6, 182, 212, 0.05)` (cyan line unchanged)
  </acceptance_criteria>
  <done>All CSS variables remapped to navy, POC-pattern tokens defined in both :root and .dark, Montserrat set as body font, surface-grid rgba updated to navy</done>
</task>

<task type="auto">
  <name>Task 2: Google Fonts link + indigo sweep verification</name>
  <files>frontend/index.html</files>
  <read_first>
    - frontend/index.html (current state — 13 lines, no Google Fonts links yet)
    - .planning/phases/07-brand-foundation/07-RESEARCH.md (Pattern 2 — canonical three-tag Google Fonts pattern, Pitfall 3)
  </read_first>
  <action>
**1. Add Google Fonts links to index.html (BRAND-02, per D-01):**

Insert 3 `<link>` tags inside `<head>`, after the `<title>` tag and before `</head>`:

```html
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&display=swap" rel="stylesheet">
```

Load weights 400, 500, 600, 700 (covers regular, medium, semibold, bold used across the app). Use `display=swap` to prevent invisible text during load.

CRITICAL: The `fonts.gstatic.com` preconnect MUST have the `crossorigin` attribute — without it, font files are not preloaded (no error, just slower).

**2. Indigo/purple/violet sweep verification (BRAND-01, per D-07):**

Run `grep -r "indigo\|purple\|violet" frontend/src/` to verify zero matches. Research confirmed zero matches as of scan — this step is a required verification gate, not an active replacement task. If any matches ARE found (unexpected), replace `indigo-*` classes with `primary` equivalents (e.g., `bg-indigo-600` -> `bg-primary`, `text-indigo-600` -> `text-primary`, `border-indigo-500` -> `border-primary`, `hover:bg-indigo-700` -> `hover:bg-primary/90`).

**3. Run existing test suite to confirm no regressions:**

Run `cd frontend && npm test -- --run` — all existing tests must pass.
  </action>
  <verify>
    <automated>grep -q "fonts.googleapis.com" frontend/index.html && grep -q 'crossorigin' frontend/index.html && grep -q "Montserrat" frontend/index.html && test $(grep -r "indigo\|purple\|violet" frontend/src/ | wc -l) -eq 0 && cd frontend && npm test -- --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/index.html` contains `<link rel="preconnect" href="https://fonts.googleapis.com">`
    - `frontend/index.html` contains `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>`
    - `frontend/index.html` contains `fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&display=swap`
    - `grep -r "indigo\|purple\|violet" frontend/src/` returns zero matches
    - `cd frontend && npm test -- --run` exits with code 0 (all existing tests pass)
  </acceptance_criteria>
  <done>Google Fonts Montserrat loaded via CDN with preconnect, indigo sweep verified clean, existing test suite green</done>
</task>

</tasks>

<verification>
1. `grep -c "217 60% 25%" frontend/src/styles.css` returns `4` (--primary x2 + --ring x2)
2. `grep "color-brand" frontend/src/styles.css` shows entries in both :root and .dark blocks
3. `grep "Montserrat" frontend/src/styles.css frontend/index.html` shows font-family declaration and CDN link
4. `grep "rgba(99, 102, 241" frontend/src/styles.css` returns zero matches (old indigo gone)
5. `grep -r "indigo\|purple\|violet" frontend/src/` returns zero matches
6. `cd frontend && npm test -- --run` passes with exit code 0
</verification>

<success_criteria>
- Every `bg-primary`, `text-primary`, `ring-primary`, `border-primary` in the app resolves to navy #1a3868 via CSS variable cascade
- Body text renders in Montserrat (Google Fonts CDN loaded, font-family set)
- All 5 POC-pattern CSS variables are defined in both :root and .dark
- Zero hardcoded indigo/purple/violet Tailwind classes in frontend/src/
- Existing frontend test suite passes without regressions
</success_criteria>

<output>
After completion, create `.planning/phases/07-brand-foundation/07-01-SUMMARY.md`
</output>
