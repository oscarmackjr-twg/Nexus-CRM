---
phase: 04-deal-model-expansion-fund-entity
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - alembic/versions/0008_deal_pe_fields.py
  - alembic/versions/0009_deal_team_members.py
  - backend/models.py
autonomous: true
requirements: [DEAL-01, DEAL-02, DEAL-03, DEAL-04, DEAL-05, DEAL-06, DEAL-07, DEAL-08, DEAL-09]

must_haves:
  truths:
    - "Deal table has all PE columns: description, new_deal_date, transaction_type_id, fund_id, platform_or_addon, platform_company_id, all source tracking FKs, all 6 financial amount+currency pairs, all 8 date milestones, passed_dead fields, legacy_id"
    - "deal_team_members M2M table exists with composite PK (deal_id, user_id)"
    - "All new columns are nullable per D-13"
    - "All ref_data FK columns use ondelete=SET NULL per D-12"
    - "legacy_id has org-scoped unique constraint"
  artifacts:
    - path: "alembic/versions/0008_deal_pe_fields.py"
      provides: "All PE columns on deals table"
      contains: "op.add_column"
    - path: "alembic/versions/0009_deal_team_members.py"
      provides: "deal_team_members M2M association table"
      contains: "op.create_table"
    - path: "backend/models.py"
      provides: "Updated Deal class with all PE columns + DealTeamMember class"
      contains: "class DealTeamMember"
  key_links:
    - from: "alembic/versions/0008_deal_pe_fields.py"
      to: "alembic/versions/0007_fund.py"
      via: "down_revision chain"
      pattern: "down_revision.*0007_fund"
    - from: "backend/models.py Deal class"
      to: "backend/models.py Fund class"
      via: "fund_id FK + fund relationship"
      pattern: "fund_id.*ForeignKey.*funds.id"
---

<objective>
Add all PE transaction fields to the Deal model via two Alembic migrations and update the ORM model with ~30 new columns plus the deal_team_members M2M table.

Purpose: This is the core data model expansion for Phase 4 — every subsequent plan (service, API, UI) depends on these columns existing.
Output: Two migrations (0008, 0009) and updated Deal ORM model with all PE Blueprint fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deal-model-expansion-fund-entity/04-CONTEXT.md
@.planning/phases/04-deal-model-expansion-fund-entity/04-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/04-deal-model-expansion-fund-entity/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: Fund table and migration chain -->
From alembic/versions/0007_fund.py:
```python
revision = "0007_fund"
down_revision = "0006_activity_deal_id_nullable"
# funds table with id String(36) PK
```

From backend/models.py (existing Deal class, lines 297-337):
```python
class Deal(Base):
    __tablename__ = "deals"
    __table_args__ = (
        Index("ix_deals_org_team", "org_id", "team_id"),
        Index("ix_deals_pipeline_stage", "pipeline_id", "stage_id"),
        Index("ix_deals_org_status", "org_id", "status"),
    )
    id: Mapped[UUID] = mapped_column(primary_key=True, default=uuid4)
    org_id: Mapped[UUID] = mapped_column(ForeignKey("organizations.id", ondelete="CASCADE"), nullable=False)
    # ... existing columns through updated_at
    org: Mapped[Organization] = relationship(back_populates="deals")
    # ... existing relationships
    activities: Mapped[list[DealActivity]] = relationship(back_populates="deal", cascade="all, delete-orphan")
```

From alembic/versions/0003_contact_pe_fields.py (add_column + FK pattern):
```python
op.add_column("contacts", sa.Column("contact_type_id", sa.String(36), nullable=True))
op.create_foreign_key("fk_contacts_contact_type_id", "contacts", "ref_data", ["contact_type_id"], ["id"], ondelete="SET NULL")
```

From backend/models.py (ContactCoveragePerson M2M pattern):
```python
class ContactCoveragePerson(Base):
    __tablename__ = "contact_coverage_persons"
    contact_id: Mapped[str] = mapped_column(String(36), ForeignKey("contacts.id", ondelete="CASCADE"), primary_key=True)
    user_id: Mapped[str] = mapped_column(String(36), ForeignKey("users.id", ondelete="CASCADE"), primary_key=True)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deal PE fields migration (0008) and deal_team_members migration (0009)</name>
  <files>
    alembic/versions/0008_deal_pe_fields.py,
    alembic/versions/0009_deal_team_members.py
  </files>
  <read_first>
    alembic/versions/0007_fund.py,
    alembic/versions/0003_contact_pe_fields.py,
    alembic/versions/0004_contact_coverage_persons.py,
    alembic/versions/0005_company_pe_fields.py,
    backend/models.py
  </read_first>
  <action>
    **Migration 0008_deal_pe_fields.py:**
    - `revision = "0008_deal_pe_fields"`
    - `down_revision = "0007_fund"`

    upgrade() adds all these columns to the `deals` table (ALL nullable per D-13):

    **Identity fields (DEAL-01, DEAL-03):**
    - `description` Text nullable=True
    - `new_deal_date` Date nullable=True
    - `transaction_type_id` String(36) nullable=True + FK to ref_data.id ondelete=SET NULL (fk name: `fk_deals_transaction_type_id`)
    - `fund_id` String(36) nullable=True + FK to funds.id ondelete=SET NULL (fk name: `fk_deals_fund_id`)
    - `platform_or_addon` String(20) nullable=True (stores "platform", "addon", or null — NOT an enum, per research anti-pattern)
    - `platform_company_id` String(36) nullable=True + FK to companies.id ondelete=SET NULL (fk name: `fk_deals_platform_company_id`)

    **Source tracking fields (DEAL-04):**
    - `source_type_id` String(36) nullable=True + FK to ref_data.id ondelete=SET NULL (fk name: `fk_deals_source_type_id`)
    - `source_company_id` String(36) nullable=True + FK to companies.id ondelete=SET NULL (fk name: `fk_deals_source_company_id`)
    - `source_individual_id` String(36) nullable=True + FK to contacts.id ondelete=SET NULL (fk name: `fk_deals_source_individual_id`)
    - `originator_id` String(36) nullable=True + FK to users.id ondelete=SET NULL (fk name: `fk_deals_originator_id`)

    **Financial fields (DEAL-05) — each is Numeric(18,2) + String(3) pair:**
    - `revenue_amount` Numeric(18,2) nullable=True, `revenue_currency` String(3) nullable=True
    - `ebitda_amount` Numeric(18,2) nullable=True, `ebitda_currency` String(3) nullable=True
    - `enterprise_value_amount` Numeric(18,2) nullable=True, `enterprise_value_currency` String(3) nullable=True
    - `equity_investment_amount` Numeric(18,2) nullable=True, `equity_investment_currency` String(3) nullable=True

    **Bid fields (DEAL-06):**
    - `loi_bid_amount` Numeric(18,2) nullable=True, `loi_bid_currency` String(3) nullable=True
    - `ioi_bid_amount` Numeric(18,2) nullable=True, `ioi_bid_currency` String(3) nullable=True

    **Date milestone fields (DEAL-07) — all Date nullable=True:**
    - `cim_received_date`
    - `ioi_due_date`
    - `ioi_submitted_date`
    - `management_presentation_date`
    - `loi_due_date`
    - `loi_submitted_date`
    - `live_diligence_date`
    - `portfolio_company_date`

    **Passed/dead fields (DEAL-08):**
    - `passed_dead_date` Date nullable=True
    - `passed_dead_reasons` JSON nullable=True (use `sa.JSON` not `sa.JSONB` — SQLite compatibility per research pitfall 7)
    - `passed_dead_commentary` Text nullable=True

    **Legacy ID (DEAL-09):**
    - `legacy_id` String(100) nullable=True
    - `op.create_unique_constraint("uq_deals_org_legacy_id", "deals", ["org_id", "legacy_id"])`

    downgrade(): Drop unique constraint first, then drop all FK constraints by name, then drop all columns in reverse order.

    **Migration 0009_deal_team_members.py:**
    - `revision = "0009_deal_team_members"`
    - `down_revision = "0008_deal_pe_fields"`

    upgrade():
    ```python
    op.create_table(
        "deal_team_members",
        sa.Column("deal_id", sa.String(36), sa.ForeignKey("deals.id", ondelete="CASCADE"), primary_key=True),
        sa.Column("user_id", sa.String(36), sa.ForeignKey("users.id", ondelete="CASCADE"), primary_key=True),
    )
    op.create_index("ix_deal_team_members_deal_id", "deal_team_members", ["deal_id"])
    op.create_index("ix_deal_team_members_user_id", "deal_team_members", ["user_id"])
    ```

    downgrade():
    ```python
    op.drop_index("ix_deal_team_members_user_id")
    op.drop_index("ix_deal_team_members_deal_id")
    op.drop_table("deal_team_members")
    ```
  </action>
  <verify>
    <automated>cd backend && python -c "from alembic.config import Config; from alembic import command; c = Config('alembic.ini'); command.check(c)" 2>&1 | head -5 || echo "alembic check done"</automated>
  </verify>
  <acceptance_criteria>
    - alembic/versions/0008_deal_pe_fields.py contains `revision = "0008_deal_pe_fields"`
    - alembic/versions/0008_deal_pe_fields.py contains `down_revision = "0007_fund"`
    - alembic/versions/0008_deal_pe_fields.py contains `op.add_column("deals", sa.Column("description"`
    - alembic/versions/0008_deal_pe_fields.py contains `op.add_column("deals", sa.Column("fund_id"`
    - alembic/versions/0008_deal_pe_fields.py contains `op.add_column("deals", sa.Column("revenue_amount"`
    - alembic/versions/0008_deal_pe_fields.py contains `op.add_column("deals", sa.Column("cim_received_date"`
    - alembic/versions/0008_deal_pe_fields.py contains `op.add_column("deals", sa.Column("passed_dead_reasons"`
    - alembic/versions/0008_deal_pe_fields.py contains `op.add_column("deals", sa.Column("legacy_id"`
    - alembic/versions/0008_deal_pe_fields.py contains `uq_deals_org_legacy_id`
    - alembic/versions/0008_deal_pe_fields.py contains `def downgrade`
    - alembic/versions/0009_deal_team_members.py contains `revision = "0009_deal_team_members"`
    - alembic/versions/0009_deal_team_members.py contains `down_revision = "0008_deal_pe_fields"`
    - alembic/versions/0009_deal_team_members.py contains `op.create_table("deal_team_members"`
    - alembic/versions/0009_deal_team_members.py contains `def downgrade`
  </acceptance_criteria>
  <done>Both migrations exist with correct revision chain. 0008 adds all ~30 PE columns to deals. 0009 creates deal_team_members M2M table.</done>
</task>

<task type="auto">
  <name>Task 2: Deal ORM model expansion + DealTeamMember class</name>
  <files>
    backend/models.py
  </files>
  <read_first>
    backend/models.py,
    alembic/versions/0008_deal_pe_fields.py,
    alembic/versions/0009_deal_team_members.py
  </read_first>
  <action>
    **Add DealTeamMember class** to backend/models.py BEFORE the Deal class (same pattern as ContactCoveragePerson before Contact):
    ```python
    class DealTeamMember(Base):
        __tablename__ = "deal_team_members"
        deal_id: Mapped[str] = mapped_column(
            String(36), ForeignKey("deals.id", ondelete="CASCADE"), primary_key=True
        )
        user_id: Mapped[str] = mapped_column(
            String(36), ForeignKey("users.id", ondelete="CASCADE"), primary_key=True
        )
    ```

    **Expand Deal class** — add all new Mapped columns after `updated_at` (line ~327), before relationships:

    **Identity:**
    ```python
    description: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
    new_deal_date: Mapped[Optional[date]] = mapped_column(Date, nullable=True)
    transaction_type_id: Mapped[Optional[str]] = mapped_column(String(36), ForeignKey("ref_data.id", ondelete="SET NULL"), nullable=True)
    fund_id: Mapped[Optional[str]] = mapped_column(String(36), ForeignKey("funds.id", ondelete="SET NULL"), nullable=True)
    platform_or_addon: Mapped[Optional[str]] = mapped_column(String(20), nullable=True)
    platform_company_id: Mapped[Optional[str]] = mapped_column(String(36), ForeignKey("companies.id", ondelete="SET NULL"), nullable=True)
    ```

    **Source tracking:**
    ```python
    source_type_id: Mapped[Optional[str]] = mapped_column(String(36), ForeignKey("ref_data.id", ondelete="SET NULL"), nullable=True)
    source_company_id: Mapped[Optional[str]] = mapped_column(String(36), ForeignKey("companies.id", ondelete="SET NULL"), nullable=True)
    source_individual_id: Mapped[Optional[str]] = mapped_column(String(36), ForeignKey("contacts.id", ondelete="SET NULL"), nullable=True)
    originator_id: Mapped[Optional[str]] = mapped_column(String(36), ForeignKey("users.id", ondelete="SET NULL"), nullable=True)
    ```

    **Financials (6 pairs):**
    ```python
    revenue_amount: Mapped[Optional[Decimal]] = mapped_column(Numeric(18, 2), nullable=True)
    revenue_currency: Mapped[Optional[str]] = mapped_column(String(3), nullable=True)
    ebitda_amount: Mapped[Optional[Decimal]] = mapped_column(Numeric(18, 2), nullable=True)
    ebitda_currency: Mapped[Optional[str]] = mapped_column(String(3), nullable=True)
    enterprise_value_amount: Mapped[Optional[Decimal]] = mapped_column(Numeric(18, 2), nullable=True)
    enterprise_value_currency: Mapped[Optional[str]] = mapped_column(String(3), nullable=True)
    equity_investment_amount: Mapped[Optional[Decimal]] = mapped_column(Numeric(18, 2), nullable=True)
    equity_investment_currency: Mapped[Optional[str]] = mapped_column(String(3), nullable=True)
    loi_bid_amount: Mapped[Optional[Decimal]] = mapped_column(Numeric(18, 2), nullable=True)
    loi_bid_currency: Mapped[Optional[str]] = mapped_column(String(3), nullable=True)
    ioi_bid_amount: Mapped[Optional[Decimal]] = mapped_column(Numeric(18, 2), nullable=True)
    ioi_bid_currency: Mapped[Optional[str]] = mapped_column(String(3), nullable=True)
    ```

    **Date milestones (8 dates):**
    ```python
    cim_received_date: Mapped[Optional[date]] = mapped_column(Date, nullable=True)
    ioi_due_date: Mapped[Optional[date]] = mapped_column(Date, nullable=True)
    ioi_submitted_date: Mapped[Optional[date]] = mapped_column(Date, nullable=True)
    management_presentation_date: Mapped[Optional[date]] = mapped_column(Date, nullable=True)
    loi_due_date: Mapped[Optional[date]] = mapped_column(Date, nullable=True)
    loi_submitted_date: Mapped[Optional[date]] = mapped_column(Date, nullable=True)
    live_diligence_date: Mapped[Optional[date]] = mapped_column(Date, nullable=True)
    portfolio_company_date: Mapped[Optional[date]] = mapped_column(Date, nullable=True)
    ```

    **Passed/dead:**
    ```python
    passed_dead_date: Mapped[Optional[date]] = mapped_column(Date, nullable=True)
    passed_dead_reasons: Mapped[Optional[list]] = mapped_column(JSONVariant, nullable=True)
    passed_dead_commentary: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
    ```

    **Legacy ID:**
    ```python
    legacy_id: Mapped[Optional[str]] = mapped_column(String(100), nullable=True)
    ```

    **Add __table_args__ entries** — add `UniqueConstraint("org_id", "legacy_id", name="uq_deals_org_legacy_id")` to existing __table_args__ tuple.

    **Add relationships** after existing relationships:
    ```python
    fund: Mapped[Optional["Fund"]] = relationship(back_populates="deals")
    deal_team_members: Mapped[list["DealTeamMember"]] = relationship(cascade="all, delete-orphan")
    platform_company: Mapped[Optional["Company"]] = relationship(foreign_keys=[platform_company_id])
    source_company: Mapped[Optional["Company"]] = relationship(foreign_keys=[source_company_id])
    source_individual: Mapped[Optional["Contact"]] = relationship(foreign_keys=[source_individual_id])
    originator: Mapped[Optional["User"]] = relationship(foreign_keys=[originator_id])
    ```

    Note: The `company` relationship already exists for the main company_id. The new `platform_company` and `source_company` relationships must use `foreign_keys=[...]` to disambiguate since Deal now has 3 FKs pointing to companies.
  </action>
  <verify>
    <automated>cd backend && python -c "from backend.models import Deal, DealTeamMember, Fund; print('Deal columns:', len(Deal.__table__.columns)); print('DealTeamMember OK'); print('Fund OK')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/models.py contains `class DealTeamMember(Base):`
    - backend/models.py contains `description: Mapped[Optional[str]] = mapped_column(Text`
    - backend/models.py contains `fund_id: Mapped[Optional[str]] = mapped_column(String(36), ForeignKey("funds.id"`
    - backend/models.py contains `transaction_type_id: Mapped[Optional[str]] = mapped_column(String(36), ForeignKey("ref_data.id"`
    - backend/models.py contains `source_type_id: Mapped[Optional[str]]`
    - backend/models.py contains `revenue_amount: Mapped[Optional[Decimal]]`
    - backend/models.py contains `cim_received_date: Mapped[Optional[date]]`
    - backend/models.py contains `passed_dead_reasons: Mapped[Optional[list]] = mapped_column(JSONVariant`
    - backend/models.py contains `legacy_id: Mapped[Optional[str]]`
    - backend/models.py contains `uq_deals_org_legacy_id`
    - backend/models.py contains `fund: Mapped[Optional["Fund"]]`
    - backend/models.py contains `deal_team_members: Mapped[list["DealTeamMember"]]`
  </acceptance_criteria>
  <done>Deal ORM model has all ~30 PE columns, DealTeamMember M2M class exists, Deal has relationships for fund, platform_company, source_company, source_individual, originator, and deal_team_members. Python import of models succeeds.</done>
</task>

</tasks>

<verification>
- `cd backend && python -c "from backend.models import Deal, DealTeamMember, Fund; print('All models import OK')"` succeeds
- `cd backend && python -m pytest tests/test_health.py -x -q` passes
- `cd backend && python -m pytest tests/test_crm_core.py -x -q` passes (existing tests unbroken)
</verification>

<success_criteria>
- All 30+ PE columns exist on Deal ORM model per DEAL-01 through DEAL-09
- DealTeamMember M2M class exists per DEAL-02
- Migration chain is 0007 -> 0008 -> 0009
- All new columns are nullable per D-13
- All ref_data FK columns use ondelete=SET NULL per D-12
- passed_dead_reasons uses JSONVariant per D-14
- legacy_id has org-scoped unique constraint per DEAL-09
- Existing tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/04-deal-model-expansion-fund-entity/04-02-SUMMARY.md`
</output>
