---
phase: 18-access-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/auth/access.py
  - backend/services/_crm.py
  - backend/tests/conftest.py
  - backend/tests/test_access_enforcement.py
autonomous: true
requirements: [ACCESS-01, ACCESS-02, ACCESS-05]
must_haves:
  truths:
    - "Admin and Principal see deals from every group in list results (no team filter)"
    - "Regular User and Supervisor list results contain only their own group's deals"
    - "A Supervisor sees a same-group teammate's is_private deal in list results; a Regular User does not"
    - "Contacts and Companies list endpoints return 200 for every role regardless of group"
  artifacts:
    - path: "backend/auth/access.py"
      provides: "Single authz source of truth — role predicates, visible_deal_team_ids, private_deal_predicate, can_read/write/delete_deal, require_deal_readable/writable"
      contains: "def visible_deal_team_ids"
      min_lines: 80
    - path: "backend/services/_crm.py"
      provides: "accessible_team_ids and private_deal_predicate delegate to access.py"
      contains: "from backend.auth.access import"
    - path: "backend/tests/test_access_enforcement.py"
      provides: "List/visibility + global-read test matrix"
      contains: "def test_"
    - path: "backend/tests/conftest.py"
      provides: "seeded_org fixture includes a principal-role user"
      contains: "principal"
  key_links:
    - from: "backend/services/_crm.py::accessible_team_ids"
      to: "backend/auth/access.py::visible_deal_team_ids"
      via: "delegation import"
      pattern: "visible_deal_team_ids"
    - from: "backend/services/deals.py::_base_deal_stmt"
      to: "backend/services/_crm.py::private_deal_predicate"
      via: "WHERE clause composition (unchanged call site, corrected behavior)"
      pattern: "private_deal_predicate"
---

<objective>
Build the centralized authorization module (`backend/auth/access.py`) that is the single source of truth for the four-role access model, and rewrite the two broken `_crm.py` helpers (`accessible_team_ids`, `private_deal_predicate`) to delegate to it. Correcting these two helpers fixes list/read scoping for every deal query automatically, because they feed `DealService._base_deal_stmt`.

This plan also lays the test foundation for the whole phase: it adds the missing `principal` user to the `seeded_org` fixture and creates `backend/tests/test_access_enforcement.py` with the list-visibility and global-read portions of the role × action matrix.

Purpose: Establish the authz contract (D-14, D-15, D-16) that plans 02 and 03 consume verbatim, and make Admin/Principal cross-group visibility actually work (currently broken — `accessible_team_ids` returns `[user.team_id]` for everyone).
Output: `backend/auth/access.py`, rewritten `_crm.py` delegating wrappers, extended `conftest.py` fixture, and `test_access_enforcement.py` list/visibility tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-access-enforcement/18-CONTEXT.md
@.planning/phases/18-access-enforcement/18-RESEARCH.md
@.planning/phases/18-access-enforcement/18-PATTERNS.md

<interfaces>
<!-- Contracts this plan CREATES. Plans 02 and 03 import these verbatim from backend/auth/access.py. -->
<!-- Role string values (from Phase 17): "regular_user", "supervisor", "principal", "admin" -->

backend/auth/access.py public surface (Task 1 must create exactly these signatures):

  def is_admin(user) -> bool
  def is_principal(user) -> bool
  def is_supervisor(user) -> bool
  def is_oversight_role(user) -> bool            # role in ("supervisor","principal","admin")

  def visible_deal_team_ids(user) -> list[UUID] | None
      # None  -> no team filter (admin, principal see ALL teams)
      # []    -> user.team_id is None -> sees nothing
      # [id]  -> regular_user / supervisor -> own team only

  def private_deal_predicate(user)               # returns True (no-op clause) for oversight roles,
                                                 # else or_(Deal.is_private.is_(False), Deal.owner_id == user.id)

  def can_read_deal(user, deal) -> bool
  def can_write_deal(user, deal) -> bool
  def can_delete_deal(user, deal) -> bool

  async def require_deal_readable(db, deal_id, user) -> Deal   # 404 if absent, 403 if forbidden
  async def require_deal_writable(db, deal_id, user) -> Deal   # 404/403, then can_write_deal gate

Existing _crm.py helpers that STAY (do not remove): is_admin, is_manager_plus,
ensure_owner_or_admin, clamp_pagination, count_rows, page_count, ensure_*_in_org.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend/auth/access.py authz module (contract for whole phase)</name>
  <files>backend/auth/access.py</files>
  <read_first>
    - backend/auth/security.py (copy module header + `raise HTTPException(status_code=status.HTTP_403_FORBIDDEN, ...)` pattern from require_org_admin)
    - backend/services/_crm.py (copy the `getattr(user, "role", None) in (...)` role-check style at lines 112-117; copy the async load + 404 pattern from ensure_company_in_org lines 54-61; current private_deal_predicate lines 151-155)
    - backend/models.py (confirm Deal.team_id, Deal.owner_id, Deal.is_private, Deal.org_id field names)
    - .planning/phases/18-access-enforcement/18-RESEARCH.md (## Architecture Patterns, Pattern 1 — full reference implementation of every function)
  </read_first>
  <action>
    Create new file backend/auth/access.py. First line must be `from __future__ import annotations`. Import UUID, `select`/`or_` from sqlalchemy, AsyncSession, and `HTTPException`/`status` from fastapi. Import the `Deal` model lazily inside functions (mirror the `from backend.models import ...` inside-function pattern used throughout _crm.py) to avoid circular imports.

    Implement EXACTLY the public surface listed in the `<interfaces>` block above using role string values `regular_user`/`supervisor`/`principal`/`admin`:
    - Role predicates via `getattr(user, "role", None)` checks. `is_oversight_role` returns True for supervisor/principal/admin.
    - `visible_deal_team_ids(user)`: return None when `is_admin(user) or is_principal(user)`; return `[]` when `user.team_id is None`; else return `[user.team_id]`. (Per D-05/D-06: admin & principal see all; supervisor & regular_user own team only.)
    - `private_deal_predicate(user)`: return literal `True` for `is_oversight_role(user)` (no-op WHERE clause — D-11 oversight roles are not blinded by is_private); else `or_(Deal.is_private.is_(False), Deal.owner_id == user.id)`.
    - `can_read_deal(user, deal)`: admin/principal -> True; if `user.team_id is None` or `deal.team_id != user.team_id` -> False; if `user.role == "regular_user"` and `deal.is_private` and `deal.owner_id != user.id` -> False; else True.
    - `can_write_deal(user, deal)`: admin -> True; supervisor -> `user.team_id is not None and deal.team_id == user.team_id` (D-08 same-team edit); else (principal, regular_user) -> `deal.owner_id == user.id` (D-06/D-10 own records only).
    - `can_delete_deal(user, deal)`: admin -> True; else `deal.owner_id == user.id` (D-07 owner-or-admin; D-08 supervisor cannot delete others').
    - `require_deal_readable(db, deal_id, user)`: load `Deal` with `Deal.id == deal_id AND Deal.org_id == user.org_id` ONLY (no team filter — this is the existence check). If None -> raise 404 "Deal not found". If `not can_read_deal(user, deal)` -> raise 403 "Forbidden". Return deal. (D-01/D-02 clean 403-vs-404 split.)
    - `require_deal_writable(db, deal_id, user)`: `deal = await require_deal_readable(...)`; if `not can_write_deal(user, deal)` -> raise 403; return deal.

    Do NOT add Call/Note logic or stubs (D-21 — Phase 19 registers them later). Do NOT inline implementations into this action; the reference code lives in 18-RESEARCH.md Pattern 1.
  </action>
  <verify>
    <automated>cd backend && python -c "from backend.auth import access; assert hasattr(access,'visible_deal_team_ids') and hasattr(access,'require_deal_readable') and hasattr(access,'can_write_deal') and hasattr(access,'is_oversight_role')"</automated>
  </verify>
  <acceptance_criteria>
    - File backend/auth/access.py exists and its first line is `from __future__ import annotations`.
    - `access.py` contains `def visible_deal_team_ids(`, `def require_deal_readable(`, `def require_deal_writable(`, `def can_read_deal(`, `def can_write_deal(`, `def can_delete_deal(`, `def is_oversight_role(`.
    - Module imports cleanly with no circular-import error (verify command exits 0).
    - No reference to Call or Note entities anywhere in the file.
  </acceptance_criteria>
  <done>access.py defines all functions in the interfaces block; importable; no Call/Note code.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite _crm.py accessible_team_ids and private_deal_predicate to delegate</name>
  <files>backend/services/_crm.py</files>
  <read_first>
    - backend/services/_crm.py (current broken accessible_team_ids lines 39-43; current broken private_deal_predicate lines 151-155 — these are the ONLY two functions that change)
    - backend/services/deals.py lines 56-60 (_visible_team_ids calls `await accessible_team_ids(self.db, user)`; _base_deal_stmt consumes the result and applies private_deal_predicate — confirm None means "no team filter")
    - backend/auth/access.py (the functions created in Task 1)
  </read_first>
  <action>
    In backend/services/_crm.py, replace the body of `accessible_team_ids(session, user)` so it delegates: `from backend.auth.access import visible_deal_team_ids` then `return visible_deal_team_ids(user)`. Keep the function `async` and keep both the `session` and `user` parameters (zero call-site disruption per Pitfall 6). Update the return annotation to `list[UUID] | None`.

    Replace the body of `private_deal_predicate(user, visible_team_ids=None)` so it delegates: `from backend.auth.access import private_deal_predicate as _authz_predicate` then `return _authz_predicate(user)`. Keep the unused `visible_team_ids` parameter for signature compatibility (callers still pass it).

    Change NOTHING else in _crm.py — `is_admin`, `is_manager_plus`, `ensure_owner_or_admin`, pagination helpers, and all `ensure_*_in_org` functions stay exactly as-is. Confirm by reading deals.py that `_base_deal_stmt` already branches on `visible_team_ids is None` (no team filter) vs a list (applies `Deal.team_id.in_(...)`); do not modify `_base_deal_stmt` structure — it becomes correct once these two helpers return the right values.
  </action>
  <verify>
    <automated>cd backend && python -c "from backend.services._crm import accessible_team_ids, private_deal_predicate; from backend.auth import access; import inspect; assert 'visible_deal_team_ids' in inspect.getsource(accessible_team_ids); assert inspect.iscoroutinefunction(accessible_team_ids)"</automated>
  </verify>
  <acceptance_criteria>
    - `_crm.py` contains `from backend.auth.access import visible_deal_team_ids` inside `accessible_team_ids`.
    - `accessible_team_ids` is still an async function and still accepts `(session, user)`.
    - `private_deal_predicate(user, visible_team_ids=None)` delegates to `backend.auth.access.private_deal_predicate`.
    - `is_admin`, `is_manager_plus`, `ensure_owner_or_admin` are unchanged (still present, same signatures).
  </acceptance_criteria>
  <done>Both helpers delegate to access.py; no other _crm.py function altered; call sites in deals.py unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add principal fixture + list/visibility/global-read tests</name>
  <files>backend/tests/conftest.py, backend/tests/test_access_enforcement.py</files>
  <read_first>
    - backend/tests/conftest.py lines 160-196 (seeded_org fixture — the user tuple loop to extend; note it currently has NO principal-role user) and lines 281-298 (seed_50_deals pattern for building Deal rows)
    - backend/tests/test_authorship.py lines 1-60 (file header, `@pytest.mark.asyncio`, `async_client`, `seeded_org`, `auth_header`, `/api/v1/...` path, status_code assertions)
    - backend/services/deals.py lines 56-90 (_base_deal_stmt — what list scoping now produces)
    - .planning/phases/18-access-enforcement/18-RESEARCH.md (## Role × Action Truth Table — LIST section and CONTACTS/COMPANIES section)
  </read_first>
  <behavior>
    - Admin GET /api/v1/deals returns both alpha-team and beta-team deals (cross-group list).
    - Principal GET /api/v1/deals returns both alpha and beta deals (ACCESS-05 read-all).
    - beta-rep GET /api/v1/deals returns ONLY beta deals; alpha deals absent (ACCESS-02 silent filter, never 403 on list).
    - alpha-manager (supervisor) GET /api/v1/deals returns a same-team is_private deal owned by alpha-peer; alpha-rep (regular_user, not owner) does NOT see that private deal.
    - Every role GET /api/v1/contacts and GET /api/v1/companies returns 200 (ACCESS-01 global read).
  </behavior>
  <action>
    In backend/tests/conftest.py, add one entry to the `seeded_org` user tuple list (inside the loop at ~line 172-181): `("principal@example.com", "principal", "principal", alpha)` so `seeded_org["principal"]` exists (Wave 0 gap — required for ACCESS-05). Do not remove or rename existing users.

    Create backend/tests/test_access_enforcement.py. Copy the header/import style from test_authorship.py (`from __future__ import annotations`, pytest, pytest_asyncio, `from backend.tests.conftest import auth_header`, `from backend.models import Deal`). Add a `deal_fixtures` pytest_asyncio fixture that inserts one alpha-team deal (owner alpha-rep) and one beta-team deal (owner beta-rep) plus one alpha-team `is_private=True` deal owned by alpha-peer, all using `seeded_org`, `pipeline`, `stages` (mirror the seed_50_deals construction).

    Write the test functions covering the `<behavior>` list, asserting on response `status_code` and on the set of deal ids/names present in the JSON `items`. Use role users `seeded_org["admin"]`, `seeded_org["principal"]`, `seeded_org["beta-rep"]`, `seeded_org["alpha-manager"]`, `seeded_org["alpha-rep"]`, `seeded_org["alpha-peer"]`. Name the contacts/companies test `test_contacts_globally_readable`. Use the `/api/v1/deals`, `/api/v1/contacts`, `/api/v1/companies` paths. Assert LIST endpoints NEVER return 403 (D-03).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_access_enforcement.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `conftest.py` `seeded_org` returns a `"principal"` key whose user has role `"principal"` and team alpha.
    - `pytest backend/tests/test_access_enforcement.py -x` exits 0.
    - A test asserts beta-rep's deal list excludes alpha deals (filtered, not 403).
    - A test asserts principal's and admin's deal lists include both alpha and beta deals.
    - A test asserts a regular_user non-owner does NOT see a same-team is_private deal while the supervisor does.
    - `test_contacts_globally_readable` asserts 200 for contacts and companies across roles.
    - No LIST test expects a 403 status code.
  </acceptance_criteria>
  <done>principal fixture added; test_access_enforcement.py green; list-scoping and global-read matrix proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → deal LIST endpoints | Caller supplies JWT (role + team); server must not leak other groups' deals in list results |
| service layer → SQL predicate | `visible_deal_team_ids` / `private_deal_predicate` are the only gate translating role into a WHERE clause |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | `DealService._base_deal_stmt` list scoping | mitigate | `visible_deal_team_ids` returns `[user.team_id]` for regular_user/supervisor; other groups' deals never enter the result set (test: beta-rep list excludes alpha deals) |
| T-18-02 | Information Disclosure | `private_deal_predicate` for same-group peers | mitigate | Regular User sees private deals only when `owner_id == user.id`; oversight roles (D-11) see all — proven by supervisor-vs-regular private-deal test |
| T-18-03 | Elevation of Privilege | `visible_deal_team_ids` for admin/principal | mitigate | Returns `None` (all teams) ONLY for admin/principal; never for regular_user/supervisor — fixes the broken `[user.team_id]`-for-everyone behavior |
| T-18-04 | Information Disclosure | unassigned user (`team_id is None`) | accept→mitigate | Returns `[]` (sees nothing) rather than leaking — explicit empty-list branch |

No package-manager installs occur in this plan (no `T-18-SC` row required).
</threat_model>

<verification>
- `pytest backend/tests/test_access_enforcement.py -x -q` green.
- `pytest backend/tests/test_authorship.py -q` still green (no regression from conftest change).
- `backend/auth/access.py` imports cleanly and exposes the full interface surface.
</verification>

<success_criteria>
- access.py is the single authz source of truth and is importable with no circular imports.
- `_crm.py` helpers delegate; admin/principal now see all teams in list results; regular_user/supervisor scoped to own team.
- D-11 private-deal nuance enforced for list scoping (oversight roles not blinded).
- ACCESS-01 (global contacts/companies read), ACCESS-02 (list scoping), ACCESS-05 (principal read-all list) proven by tests.
</success_criteria>

<output>
Create `.planning/phases/18-access-enforcement/18-01-SUMMARY.md` when done.
</output>
