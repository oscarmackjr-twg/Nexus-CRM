---
phase: 06-admin-reference-data-ui
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified: []
autonomous: false
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - ADMIN-04
  - ADMIN-05
  - ADMIN-06
  - ADMIN-07
  - ADMIN-08

must_haves:
  truths:
    - "Admin page visually renders two tabs and the Reference Data tab is functional end-to-end"
    - "Adding an item in admin appears in downstream form dropdowns after invalidation"
    - "Deactivating an item removes it from form dropdowns but existing records are unaffected"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of the Admin Reference Data UI and dropdown wiring. Human verifies the complete admin workflow: tab navigation, category browsing, CRUD operations, and downstream dropdown propagation.

Purpose: Confirm the admin UI works end-to-end before closing the milestone.
Output: Human approval of Phase 6 completion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-admin-reference-data-ui/06-CONTEXT.md
@.planning/phases/06-admin-reference-data-ui/06-01-SUMMARY.md
@.planning/phases/06-admin-reference-data-ui/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and run smoke tests</name>
  <files></files>
  <read_first>
    frontend/src/pages/AdminPage.jsx
    frontend/src/lib/refCategories.js
  </read_first>
  <action>
1. Start the dev environment: `make dev` (or equivalent — check Makefile for the command).
2. Run the frontend build to confirm no build errors: `cd frontend && npx vite build --mode development`.
3. Verify AdminPage.jsx contains all expected elements:
   - `grep "TabsTrigger" frontend/src/pages/AdminPage.jsx` — expect >= 2
   - `grep "REF_CATEGORIES" frontend/src/pages/AdminPage.jsx` — expect >= 1
   - `grep "invalidateQueries" frontend/src/pages/AdminPage.jsx` — expect >= 2
   - `grep "Dialog" frontend/src/pages/AdminPage.jsx` — expect >= 1
   - `grep "opacity-50" frontend/src/pages/AdminPage.jsx` — expect >= 1
4. Verify no hardcoded ref_data values in form pages:
   - `grep -rn 'Tier 1\|Financial Sponsor\|Proprietary\|SWF\|Buyout' frontend/src/pages/ --include="*.jsx" | grep '<option'` — expect 0 matches
  </action>
  <verify>
    <automated>cd /Users/oscarmack/OpenClaw/nexus-crm/frontend && npx vite build --mode development 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Vite build completes without errors
    - grep for TabsTrigger in AdminPage.jsx returns >= 2
    - grep for hardcoded ref_data seed values in option tags returns 0 matches
  </acceptance_criteria>
  <done>Dev environment running, build passes, smoke checks pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of Admin Reference Data UI</name>
  <files></files>
  <action>
    Present the verification steps below to the human for manual testing. Wait for approval or issue reports.
  </action>
  <what-built>
    Admin page with Users/Reference Data tabs, category sidebar listing all 10 ref_data categories, items table with add/edit/deactivate/reactivate functionality, and query invalidation ensuring downstream dropdown updates.
  </what-built>
  <how-to-verify>
    Run `make dev` if not already running. Log in as admin@demo.local / password123.

    **Step 1 — Tab structure (ADMIN-01, ADMIN-02):**
    1. Navigate to http://localhost:5173/admin
    2. Verify two tabs appear: "Users" and "Reference Data"
    3. Click "Users" tab — existing Org snapshot and Users cards should display
    4. Click "Reference Data" tab — category sidebar should appear on the left

    **Step 2 — Category sidebar (ADMIN-02, ADMIN-03):**
    5. Verify all 10 categories are listed with human-readable names: Sector, Sub-Sector, Transaction Type, Tier, Contact Type, Company Type, Company Sub-Type, Deal Source Type, Passed/Dead Reason, Investor Type
    6. "Sector" should be selected by default, showing sector items in the table
    7. Click "Tier" — table should update to show Tier 1, Tier 2, Tier 3

    **Step 3 — Add item (ADMIN-04):**
    8. On the Tier category, click "+ Add Item" button
    9. Modal opens with Label and Position fields
    10. Enter Label: "Tier 4", Position: 4
    11. Click Save — item appears in the table
    12. Navigate to a Deal detail page > edit mode — verify the tier dropdown now shows "Tier 4"

    **Step 4 — Edit item (ADMIN-05):**
    13. Back on /admin > Reference Data > Tier, click the edit (pencil) icon on "Tier 4"
    14. Change label to "Tier 4 - Special"
    15. Click Save — label updates in the table

    **Step 5 — Deactivate/reactivate (ADMIN-06):**
    16. Click the deactivate (X) icon on "Tier 4 - Special"
    17. Item should turn greyed-out (opacity-50) with "Inactive" badge and a reactivate icon
    18. Navigate to a Deal detail page > edit mode — verify "Tier 4 - Special" no longer appears in tier dropdown
    19. Back on /admin > Reference Data > Tier, click the reactivate icon on "Tier 4 - Special"
    20. Item returns to normal state; verify it reappears in deal tier dropdown

    **Step 6 — Query invalidation (ADMIN-08):**
    21. Open two browser tabs: /admin and a deal detail page in edit mode
    22. In /admin, add a new sector item: "Test Sector"
    23. Switch to the deal detail tab — the sector dropdown should include "Test Sector" (may need to close/reopen the dropdown to see the refresh)
    24. In /admin, deactivate "Test Sector"
    25. Switch to deal detail — "Test Sector" should no longer appear in sector dropdown

    **Step 7 — Dropdown wiring confirmation (ADMIN-07):**
    26. Check Contact detail edit form — contact_type, sector, sub_sector dropdowns pull from ref_data (not hardcoded)
    27. Check Company detail edit form — company_type, tier, sector dropdowns pull from ref_data
    28. Check Deal detail edit form — transaction_type, deal_source_type, passed_dead_reason dropdowns pull from ref_data
    29. Check Counterparty modal — tier, investor_type dropdowns pull from ref_data
  </how-to-verify>
  <verify>Human approves or reports issues</verify>
  <acceptance_criteria>
    - Human types "approved" confirming all ADMIN-01 through ADMIN-08 requirements are met
  </acceptance_criteria>
  <done>Human has approved the Admin Reference Data UI as complete</done>
  <resume-signal>Type "approved" to complete Phase 6, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human confirms all 8 ADMIN requirements are satisfied through the verification steps above.
</verification>

<success_criteria>
- Human approves the admin reference data UI as functional
- Add/edit/deactivate/reactivate operations work end-to-end
- Query invalidation propagates changes to downstream dropdowns
- No hardcoded ref_data option lists remain in any form
- Phase 6 is complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-reference-data-ui/06-03-SUMMARY.md`
</output>
