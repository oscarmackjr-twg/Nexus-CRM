---
phase: 06-admin-reference-data-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/ContactDetailPage.jsx
  - frontend/src/pages/CompanyDetailPage.jsx
  - frontend/src/pages/DealDetailPage.jsx
  - frontend/src/pages/ContactsPage.jsx
autonomous: true
requirements:
  - ADMIN-07

must_haves:
  truths:
    - "No hardcoded ref_data option arrays remain in any form across Contact, Company, Deal, DealCounterparty, or DealFunding pages"
    - "All ref_data FK dropdowns use RefSelect component or useRefData hook with queryKey ['ref', category]"
  artifacts:
    - path: "frontend/src/pages/ContactDetailPage.jsx"
      provides: "Contact form using RefSelect for contact_type, sector, sub_sector"
      contains: "RefSelect"
    - path: "frontend/src/pages/CompanyDetailPage.jsx"
      provides: "Company form using RefSelect for company_type, tier, sector, sub_sector, company_sub_type, transaction_type"
      contains: "RefSelect"
    - path: "frontend/src/pages/DealDetailPage.jsx"
      provides: "Deal form using RefSelect for transaction_type, deal_source_type, passed_dead_reason, tier, investor_type"
      contains: "RefSelect"
  key_links:
    - from: "frontend/src/pages/*DetailPage.jsx"
      to: "@/components/RefSelect"
      via: "import and usage in edit forms"
      pattern: "RefSelect category="
---

<objective>
Audit all form dropdowns across Contact, Company, Deal, DealCounterparty, and DealFunding pages to confirm no hardcoded option lists exist for ref_data categories. Replace any found with RefSelect. This is the ADMIN-07 dropdown wiring audit.

Purpose: Ensures all ref_data dropdowns across the app are wired to the live ref_data API, so admin changes propagate to all forms.
Output: All ref_data dropdowns confirmed or fixed to use RefSelect with consistent ['ref', category] query keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-admin-reference-data-ui/06-CONTEXT.md

@frontend/src/components/RefSelect.jsx
@frontend/src/hooks/useRefData.js
@frontend/src/pages/ContactDetailPage.jsx
@frontend/src/pages/CompanyDetailPage.jsx
@frontend/src/pages/DealDetailPage.jsx
@frontend/src/pages/ContactsPage.jsx

<interfaces>
From frontend/src/components/RefSelect.jsx:
```javascript
export function RefSelect({ category, value, onChange, placeholder, disabled, className })
// Renders <Select> with options from useRefData(category)
// Options use item.id as value, item.label as display text
```

The 10 ref_data categories that MUST use RefSelect wherever they appear as dropdowns:
- sector, sub_sector, transaction_type, tier, contact_type
- company_type, company_sub_type, deal_source_type, passed_dead_reason, investor_type

Dropdowns that are NOT ref_data (entity selectors, enums) and should NOT be changed:
- Company selectors (fetching from /companies)
- Contact selectors (fetching from /contacts)
- User/team member selectors (fetching from /users)
- Fund selectors (fetching from /funds)
- Pipeline/stage selectors (fetching from /pipelines)
- Activity type selectors (call/meeting/email/note — app enum, not ref_data)
- Lifecycle stage selectors (lead/mql/sql/opportunity/customer — app enum, not ref_data)
- Platform/addon selectors ("platform"/"addon" — deal enum, not ref_data)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix ref_data dropdowns across all form pages</name>
  <files>
    frontend/src/pages/ContactDetailPage.jsx
    frontend/src/pages/CompanyDetailPage.jsx
    frontend/src/pages/DealDetailPage.jsx
    frontend/src/pages/ContactsPage.jsx
  </files>
  <read_first>
    frontend/src/pages/ContactDetailPage.jsx
    frontend/src/pages/CompanyDetailPage.jsx
    frontend/src/pages/DealDetailPage.jsx
    frontend/src/pages/ContactsPage.jsx
    frontend/src/components/RefSelect.jsx
    frontend/src/hooks/useRefData.js
  </read_first>
  <action>
Systematically audit every `<select>` and `<Select>` element in each page. For each dropdown, determine if it represents a ref_data category. If it does and is NOT already using `<RefSelect>`, replace it.

**Audit checklist by page:**

**ContactDetailPage.jsx:**
- Already imports RefSelect (line 16)
- Already uses RefSelect for contact_type (line 239), sector (line 585), sub_sector (line 620)
- Coverage person selector (line 696-712): Uses `<select>` fetching from users — this is correct, NOT ref_data. Leave as-is.
- Activity type selector (line 835-842): Hardcoded call/meeting/email/note — this is an app enum, NOT ref_data. Leave as-is.
- VERDICT: No changes needed. Confirm and document.

**CompanyDetailPage.jsx:**
- Already imports RefSelect (line 10)
- Already uses RefSelect for company_type (210), company_sub_type (240), tier (253), sector (264), sub_sector (275), sector_preferences (527), sub_sector_preferences (559), transaction_types (591)
- Coverage person selector (line 649-656): Uses `<Select>` fetching from users — correct, NOT ref_data. Leave as-is.
- VERDICT: No changes needed. Confirm and document.

**DealDetailPage.jsx:**
- Already imports RefSelect (line 14)
- Already uses RefSelect for tier (lines 295, 698), investor_type (lines 303, 387, 379), transaction_type (line 1005), deal_source_type (line 1411), passed_dead_reason (lines 1525, 1715)
- Company selectors (lines 282-289, 366-373, 685-692, 1057-1064): `<select>` fetching from companies — correct, NOT ref_data. Leave as-is.
- Fund selector (lines 1016-1023): `<select>` fetching from funds — correct, NOT ref_data. Leave as-is.
- Platform/addon selector (lines 1042-1049): Hardcoded "platform"/"addon" — app enum. Leave as-is.
- Team member selector (lines 1370-1388): `<select>` fetching from users — correct. Leave as-is.
- Originator selector (lines 1396-1403): `<select>` fetching from users — correct. Leave as-is.
- Source company (1421-1428) and source individual (1436-1443): Entity selectors. Leave as-is.
- VERDICT: No changes needed. Confirm and document.

**ContactsPage.jsx:**
- Does NOT import RefSelect
- Lifecycle stage selector (line 133): Hardcoded `stages` array = ['lead', 'marketing_qualified', 'sales_qualified', 'opportunity', 'customer']. This is an app-level enum on the Contact model, NOT a ref_data category. Leave as-is.
- Owner selector (line 134): Fetching from users. Leave as-is.
- Create form lifecycle_stage (line 185): Same enum. Leave as-is.
- VERDICT: No changes needed. Confirm and document.

**Summary:** Based on thorough grep analysis, ALL ref_data category dropdowns (contact_type, company_type, company_sub_type, sector, sub_sector, tier, transaction_type, deal_source_type, passed_dead_reason, investor_type) are already using `<RefSelect category="...">` across ContactDetailPage, CompanyDetailPage, and DealDetailPage. The DealDetailPage Counterparties and Funding tab modals also use RefSelect for tier and investor_type.

If the audit confirms no hardcoded ref_data options remain, create a brief comment at the top of each file (or skip if comments would be noise) and ensure all RefSelect usages have consistent patterns. If ANY hardcoded ref_data dropdown is found during the read, replace it with `<RefSelect category="..." value={...} onChange={...} />`.

Also verify that ALL RefSelect usages in these files pass a valid category slug from the 10 known categories. Cross-reference against: sector, sub_sector, transaction_type, tier, contact_type, company_type, company_sub_type, deal_source_type, passed_dead_reason, investor_type.
  </action>
  <verify>
    <automated>cd /Users/oscarmack/OpenClaw/nexus-crm && grep -rn "RefSelect" frontend/src/pages/ContactDetailPage.jsx frontend/src/pages/CompanyDetailPage.jsx frontend/src/pages/DealDetailPage.jsx | wc -l && echo "---" && grep -rn '<option.*Tier\|<option.*Sector\|<option.*Financial Sponsor\|<option.*Equity\|<option.*Proprietary\|<option.*LP.*GP\|<option.*SWF' frontend/src/pages/ | grep -v node_modules | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep for hardcoded ref_data values (`Tier 1`, `Financial Sponsor`, `Proprietary`, `SWF`) in `<option>` tags across frontend/src/pages/ returns zero matches
    - grep for `RefSelect` in ContactDetailPage.jsx returns >= 3 matches (contact_type, sector, sub_sector)
    - grep for `RefSelect` in CompanyDetailPage.jsx returns >= 6 matches (company_type, company_sub_type, tier, sector, sub_sector, plus preference selectors)
    - grep for `RefSelect` in DealDetailPage.jsx returns >= 5 matches (transaction_type, deal_source_type, passed_dead_reason, tier, investor_type)
    - No `<option>` tag in any page file contains a literal ref_data seed value (e.g., "Financial Services", "Tier 1", "Buyout", "Pension/Super")
    - Vite build succeeds: `cd frontend && npx vite build --mode development`
  </acceptance_criteria>
  <done>Audit complete: all ref_data category dropdowns confirmed using RefSelect with ['ref', category] query keys. No hardcoded ref_data option lists found or all found instances replaced.</done>
</task>

</tasks>

<verification>
1. `grep -rn '<option' frontend/src/pages/ContactDetailPage.jsx frontend/src/pages/CompanyDetailPage.jsx frontend/src/pages/DealDetailPage.jsx` — confirm no `<option>` contains ref_data seed values like "Tier 1", "Financial Services", "Buyout", etc.
2. `grep -c 'RefSelect' frontend/src/pages/ContactDetailPage.jsx` >= 3
3. `grep -c 'RefSelect' frontend/src/pages/CompanyDetailPage.jsx` >= 6
4. `grep -c 'RefSelect' frontend/src/pages/DealDetailPage.jsx` >= 5
5. `cd frontend && npx vite build --mode development` succeeds
</verification>

<success_criteria>
- All 10 ref_data categories that appear as form dropdowns use RefSelect or useRefData
- No hardcoded option arrays contain ref_data seed values
- All RefSelect usages have consistent category slugs matching the 10 known categories
- Vite build passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-reference-data-ui/06-02-SUMMARY.md`
</output>
